# Workspace

Interface principal do aplicativo. Ela integra `features.workspace`,
`features.spreadsheet`, `features.editor`, a prévia e o gerador compartilhado.

Execute a partir da raiz do projeto:

```bash
.venv/bin/python features/workspace/main.py
```
